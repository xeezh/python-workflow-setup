#!/bin/bash

echo "Input python version (ex. 3.10)"
read version

sudo apt install software-properties-common -y 
sudo add-apt-repository ppa:deadsnakes/ppa 
sudo apt update
sudo apt install -y mc unrar unzip python$version python$version-venv python$version-dev python3-pip build-essential libssl-dev libffi-dev
